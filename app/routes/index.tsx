export default function Index() {
  return (
    // Marquee of text, top and bottom, then animation 3js in middle, 2nd style
    <div className="flex h-screen w-screen items-center justify-center bg-gradient-to-t from-red-500 via-violet-500 to-sky-500">
      <h1 className="font-display text-6xl font-extrabold text-white">
        PlayaFM
      </h1>
    </div>
  );
}
